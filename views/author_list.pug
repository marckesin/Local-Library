extends layout

block content
  h2.ps-4.ms-2= title
  hr

  //- ul
  //-   each author in author_list
  //-     li
  //-       a(href=author.url) #{author.name}
  //-       |  (#{author.lifespan})

  //-   else
  //-     li There are no authors.

  //- div.row.row-cols-1.row-cols-md-4.g-1
  //-   each author in author_list
  //-    div.col
  //-       div.card.border-dark.mb-3
  //-         div.card-header Header
  //-         div.card-body
  //-           h5.card-title
  //-             a.link-dark(href=author.url) #{author.name}
  //-           p.card-text #{author.lifespan}

  //-   else 
  //-     p There are no authors.

  ul
    each author in author_list
      div.list-group.mb-1.w-75.item-card.shadow
        a.list-group-item.list-group-item-action.d-flex.gap-3.py-3(href=author.url aria-current="true")
          div.d-flex.gap-2.w-100.justify-content-between
            div
              h6.mb-0 #{author.name}
              p.mb-0.opacity-75 #{author.lifespan}
            small.opacity-50.text-nowrap now

    else
      li There are no book copies in this library.